# 换行符统一为 LF —— 入库是 LF,检出也是 LF。
#
# 为什么要有这个文件:
#   在此之前,这件事靠的是本机的 git config core.autocrlf=false。
#   那是**机器上的配置**,不是仓库里的约定 —— 换台机器 clone,
#   如果那台设的是 autocrlf=true,整个仓库会检出成 CRLF。
#   对 Python 运行无害,但每个文件都会显示成整文件改动,
#   diff 里看不出你到底改了什么。
#
#   放进仓库之后,这条规则跟着代码走,不再取决于谁的机器。
* text=auto eol=lf
